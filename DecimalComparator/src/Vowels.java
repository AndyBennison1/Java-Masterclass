public class Vowels {

    public static int getCount(String str) {
        int vowelsCount = 0;
        String letter;
        char[] chars = str.toCharArray();

        // your code here

        for (int n = 0; n < chars.length; n++) {
            if ((chars[n] == 'a')

                vowelsCount += 1;
            }
        else if (chars[n] == 'e')  {
            
        }
             || chars[n] == 'e' || 'i' || 'o' || 'u')) {
        }

        return vowelsCount;
    }
}